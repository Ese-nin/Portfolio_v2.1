import logo from "assets/images/logo.svg"
import React from 'react';

export const Logo = () => {
    return (
            <a href="#">
                <img src={logo} alt="logo"/>
            </a>
    );
}